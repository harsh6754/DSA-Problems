#include<bits/stdc++.h>
using namespace std;
const int MOD = 1e9 + 7;
const int MAXN = 100000;

vector<int> tree[MAXN+5];
int a [MAXN+5];
long long answer = 0;

int squreFree(int x){
    for(int p=2;p*p<=x;p++){
        int cnt = 0;
        while(x %)
    }
}